# Generated by Chef

<% node[:amanda][:backup_locations].each do |client|
  dumptype = "encrypt-tar-remote"
  if client[:hostname] == "localhost"
    dumptype = "encrypt-tar-local"
  else
    dumptype = "encrypt-tar-remote"
  end
  client[:locations].each do |location| -%>
<%=client[:hostname]%> <%=location%> <%=dumptype%>
  <% end -%>
<% end -%>
