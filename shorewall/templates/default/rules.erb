# Generated by Chef, do not edit

<% node[:shorewall][:rules].each do |r| -%>
    <% r = node[:shorewall][:rule_defaults].merge(r)-%>
<%= [
    r[:action],
    r[:source].kind_of?(Array) ? "net:#{r[:source].join(',')}" : r[:source],
    r[:destination],
    r[:protocol],
    r[:ports].join(','),
    r[:source_ports],
    r[:original_destination],
    r[:rate_limit]].join(' ') %>
<% end -%>

Ping(ACCEPT) all $FW

#LAST LINE -- ADD YOUR ENTRIES BEFORE THIS ONE -- DO NOT REMOVE
