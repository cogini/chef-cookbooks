<%= node[:gitlab][:dir] %>/log/*.log {
    rotate 7
    daily
    minsize 10M
    compress
    delaycompress
    copytruncate
}

<%= node[:gitlab][:shell][:dir] %>/gitlab-shell.log {
    rotate 7
    daily
    minsize 10M
    compress
    delaycompress
    copytruncate
}
